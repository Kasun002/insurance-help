# ─────────────────────────────────────────────────────────────────────────────
# Production runtime requirements
# Used by the Docker image (ENV=prod).
#
# Intentionally excludes:
#   chromadb         — local vector store, replaced by pgvector in prod
#   tinydb           — local article store, replaced by MongoDB in prod
#   langchain-*      — ingestion scripts only, not used by the running server
#   beautifulsoup4   — scraping scripts only
#   requests         — scraping scripts only (httpx covers runtime HTTP)
#
# For local development use requirements.txt instead.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Validation & settings
pydantic==2.9.0
pydantic-settings==2.6.0

# Embeddings (model pre-baked into Docker image at build time)
sentence-transformers==3.2.0

# LLM
google-genai>=1.0.0

# Production databases
pymongo==4.8.0
psycopg2-binary==2.9.9

# HTTP & utilities
httpx==0.27.0
python-multipart==0.0.12
python-dotenv==1.0.1
